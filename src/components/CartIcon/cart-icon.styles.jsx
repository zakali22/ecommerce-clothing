import styled from "styled-components"

export const CartIconContainer = styled.div`
	position: relative;
	height: 45px;
	width: 45px;
	display: flex;
	flex-direction: column;
	justify-content: center;
	align-items: center;
	cursor: pointer;
`;

export const CartIconNum = styled.div`
	position: absolute;
	bottom: 11px;
	font-weight: bold;
	font-size: 1rem;
`